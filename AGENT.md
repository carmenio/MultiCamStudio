# Agent Notes

MultiCamStudio is split into two production child repositories:

- `pc/` contains backend, processing, Supabase schema, and PC media serving.
- `laptop/` contains the operator UI, Expo camera app, signaling, and edge relay.

Use the parent repo only for submodule coordination and wrapper commands.
